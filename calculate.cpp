
#include <string.h>

#include "calculate.hpp"

string bullpgia::calculateBullAndPgia(string codeMaker, string codeBreaker){
	uint bulls(0), pgia(0);
	////// VARIABLES: char arrays - codeMaker and codeBreaker //////
	char cMaker[codeMaker.length()];
	strcpy(cMaker, codeMaker.c_str());
	char cBreaker[codeBreaker.length()];
	strcpy(cBreaker, codeBreaker.c_str());
	
	


	return codeBreaker;
}
